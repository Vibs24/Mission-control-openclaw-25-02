# ADR-001: npm workspaces monorepo

## Decision
Use npm workspaces for API + Web + shared types.

## Why
- Single dependency graph
- Simpler CI and local dev
- Shared models without package publishing
