apiVersion: apps/v1
kind: Deployment
metadata: {name: pulsedesk-backend, namespace: production}
spec:
  replicas: 6
  selector: {matchLabels: {app: pulsedesk-backend}}
  template:
    metadata: {labels: {app: pulsedesk-backend}}
    spec:
      containers:
      - name: backend
        image: pulsedesk/backend:prod
        ports: [{containerPort: 8080}]
