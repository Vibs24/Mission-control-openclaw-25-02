# Developer Onboarding
1. Install Docker Desktop + Node 20+
2. Copy `.env.example` to `.env`
3. `docker compose up -d --build`
4. Open frontend 5173 and backend 8080
5. Run tests: `docker compose exec backend npm test`
