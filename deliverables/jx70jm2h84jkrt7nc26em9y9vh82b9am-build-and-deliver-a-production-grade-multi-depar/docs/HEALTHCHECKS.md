# Health Checks
- Endpoint: `GET /api/health` should return `{"status":"ok","db":"ok"}`.
- Validate login and dashboard KPI render.
- Validate payroll CSV/PDF download and audit writes after operations.
