# Troubleshooting

## Login fails
Re-run seed data and use seeded credentials.

## Empty dashboard
Check seed execution and DB path (`instance/workforce.db`).

## 403 for management action
Expected for insufficient role. Use Admin/Manager.

## Export errors
Ensure dependencies installed (`fpdf2` for PDF export).
