# 2026-03-05

- Built a full Flask + SQLite Multi-Department Workforce Command Center from scratch in this workspace.
- Implemented RBAC (Admin/Manager/Reviewer), secure login/logout, employee CRUD, shift planning, attendance and leave workflows.
- Added KPI dashboard, audit timeline, notification center, advanced filters/search/pagination, CSV+PDF exports, and payroll export.
- Added scripts: setup/run/test/seed/deploy/healthcheck/rollback.
- Added automated tests (unit/integration/smoke) and verified passing in offline environment using unittest fallback.
- Created docs: architecture notes, rollback guide, README, TEST_RESULTS, QA_REPORT.
- Fixed runtime hashing issue (`scrypt` unavailable) by standardizing password hashing to `pbkdf2:sha256`.
- Fixed deploy script recursion issue when deploy target is inside project root.
- Added internal healthcheck mode for sandbox environments where local port bind is blocked.
